# Runtime-only directory. Dataset version / lineage JSON is written here by
# tests and local orchestrator runs; it is gitignored and never required by
# library imports. See .gitignore `data/metadata/`.
